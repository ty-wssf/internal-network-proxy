<?xml version="1.0" encoding="UTF-8" ?>
<view x:extends="_gen/_ProxyPortMapping.view.xml" x:schema="/nop/schema/xui/xview.xdef" xmlns:x="/nop/schema/xdsl.xdef">

    <grids>
        <grid id="list"/>
        <grid id="pick-list"/>
    </grids>

    <forms>
        <form id="view"/>
        <form id="edit">
            <layout>
                clientId[客户端]
                serverPort[服务端端口]
                protocol[协议]
                clientIp[客户端IP]
                clientPort[客户端端口]
            </layout>
        </form>
    </forms>

    <pages>
        <crud name="main"/>
        <picker name="picker"/>
    </pages>
</view>
